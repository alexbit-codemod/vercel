import { defineConfig } from "eslint/config";

export default defineConfig([
  {
    /* TODO: Import and use "@remix-run/eslint-config" - you need to find its flat config export. Example: import configName from "@remix-run/eslint-config"; then use ...configName or configName in the array */
    /* TODO: Import and use "@remix-run/eslint-config/node" - you need to find its flat config export. Example: import configName from "@remix-run/eslint-config/node"; then use ...configName or configName in the array */
    languageOptions: {
      globals: {},
      parserOptions: {}
    },
  }
]);
